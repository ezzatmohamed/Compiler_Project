#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <stdbool.h>

// atof() chars to float
// atoi() chars to int

// float to string :  snprintf(str, sizeof str, "%f", myFloat);
// int to string   :  sprintf(str, "%d", i);







